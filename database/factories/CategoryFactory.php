<?php

use Faker\Generator as Faker;
use App\Category;

$factory->define(Category::class, function (Faker $faker) {

    return [
        'title' => 'Category 1',
    ];
});

$factory->define(Category::class, function (Faker $faker) {

    return [
        'title' => 'Category 2',
    ];
});

$factory->define(Category::class, function (Faker $faker) {

    return [
        'title' => 'Category 3',
    ];
});

$factory->define(Category::class, function (Faker $faker) {

    return [
        'title' => 'Category 4',
    ];
});

$factory->define(Category::class, function (Faker $faker) {

    return [
        'title' => 'Category 5',
    ];
});

$factory->define(Category::class, function (Faker $faker) {

    return [
        'title' => 'Category 6',
    ];
});

$factory->define(Category::class, function (Faker $faker) {

    return [
        'title' => 'Category 7',
    ];
});